#!/bin/bash

# will be executed after the window manager is ready
nitrogen --restore
if [ -n $HOME/.xsession-init-guard ]; then
    fcitx
    compton &
    xscreensaver -no-splash &
    # wicd-gtk -t &
    nm-applet &
fi
